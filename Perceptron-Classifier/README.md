# Perceptron Classifier

Binary classification using the Perceptron learning rule — iteratively adjusts weights until convergence.

## Features

- Perceptron learning algorithm with configurable learning rate
- Pause, resume, and stop controls during training
- Real-time visualization of the decision boundary evolving over iterations
- Accuracy evaluation on test datasets
