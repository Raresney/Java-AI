# Median Classifier

Binary classification using the Median method — computes the centroid of each class and derives a separating hyperplane.

## Features

- Median-based classification with linear decision boundary
- Statistical analysis panel with per-attribute statistics
- Accuracy evaluation on test datasets
- Real-time visualization
